# sentence-transformers pulls in torch, which defaults to a CUDA-enabled
# build via plain `pip install` - far larger than needed on a CPU-only
# free-tier host (Render), and a real threat to fitting inside its 512MB
# RAM limit. Pin the CPU-only wheel from PyTorch's own index instead.
#
# NOT YET PINNED to an exact resolved version: this sandbox's network
# policy allows pypi.org but blocks download.pytorch.org outright (proxy
# returns 403 on CONNECT), so `pip install` here can't reach this index to
# resolve and confirm a `+cpu` build - it silently falls back to the
# CUDA build already cached from pypi.org instead. Needs a real install
# (e.g. in the Render build itself, or any environment with unrestricted
# network) to confirm the resolved version and pin it here, matching the
# exact-version-pin convention every other dependency in this file follows.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

arxiv==2.4.1
numpy==2.4.6
pymupdf==1.28.2
langchain-community==0.4.2
sentence-transformers==6.0.0
faiss-cpu==1.15.0
langchain-nvidia-ai-endpoints==1.4.3
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn==0.52.4
gradio==6.26.0
pydantic-settings==2.15.0
httpx==0.28.1
redis==8.1.0
fakeredis==2.37.1
